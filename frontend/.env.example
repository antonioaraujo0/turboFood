# Frontend TurboFood — variáveis de ambiente (Vite)
# Copie para .env (local) ou configure no painel do Render.

# URL pública do backend. Sem esta variável, o app roda em modo offline (mock).
# Local:  http://localhost:3000
# Render: https://turbofood-api.onrender.com
VITE_API_URL=http://localhost:3000
